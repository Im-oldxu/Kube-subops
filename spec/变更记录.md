# 更新日志

## 2026-05-30 v0.1.3

### 优化改进

- 为 Pod 与 Deployment 资源详情统一新增“监控”页签，复用 `KubeMonitoringChart` 时间序列图组件，避免不同资源各自实现监控样式。
- 监控页签支持目标选择、时间窗口与采样粒度控制：Pod 可查看 All Containers 或单容器，Deployment 可查看 All Pods 或单个关联 Pod；时间窗口覆盖 Last 15 min、Last 30 min、Last 1 hour、Last 6 hours、Last 12 hours 和 Today。
- CPU、Memory、Network、Disk I/O 拆分为四张图展示，并支持跨图联动 hover：同一时间点会同步显示 CPU、内存、网络入/出与磁盘读/写指标，便于排障对比。
- 明确监控数据来源与降级提示：CPU/Memory 依赖 Kubernetes Metrics Server 的 `metrics.k8s.io`；网络与磁盘 I/O 需要 Prometheus 采集 kubelet/cAdvisor 或 node_exporter 指标；未返回真实指标时不使用伪造曲线。
- 补齐图表依赖 `chart.js` 与 `vue-chartjs`，并通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build` 验证。

## 2026-05-29 v0.1.2

### 优化改进

- 完成 Pod 资源页首轮专项完善：以 Deployment 页面模式为基准补齐 Pod 列表专属列、详情弹窗核心排障字段、Pod 创建表单、表单/YAML 双向同步、YAML 创建 mock 写入和 Pod 类型字段适配，但不复制 Deployment 的 replicas、selector、strategy 等专属字段。
- Pod 列表新增重启次数、Pod IP、节点列；Pod 详情展示 phase/ready/restartCount、Pod IP、Host IP、Node、所属控制器、容器/Init 容器、镜像、端口、ServiceAccount、QoS、conditions、volumes、DNS/hostNetwork/nodeSelector/imagePullSecrets 和最近错误摘要。
- Pod 创建表单新增 `v1/Pod` schema，复用已验证的 PodSpec 区块：普通容器、Init 容器、Pod 安全、Pod 网络、Pod 存储卷、Pod 调度和节点调度；默认 YAML 生成最小 Pod 清单，不包含 Deployment 的副本和发布策略。
- Pod YAML 回填表单支持 `spec.containers`、`spec.initContainers`、volume/volumeMount、imagePullSecrets、securityContext、DNS、affinity、tolerations 等 PodSpec 字段；YAML 创建后 mock 列表会保留 Pod phase、ready、restartCount、podIP、nodeName、images 和详情 YAML。
- 同步通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build`，并通过 Playwright 验证 `/pods` 列表、详情、创建表单、默认 YAML、YAML->表单回填和提交后列表可见。
- 优化 Deployment 详情弹窗 YAML 页签可读性：YAML 展示从通用深色 `code-block` 改为与创建 / 编辑 YAML 区一致的浅色 viewer、深色文字和 YAML token 高亮，日志代码块样式保持不变。
- 为 Deployment 详情弹窗 YAML 页签补充字号控制，复用创建 / 编辑 YAML 区的 12/13/14/16 字号选项，便于查看较长或较密的 YAML 内容。
- 增强 Deployment 详情基本信息：补充展示更新策略、RollingUpdate 的 maxSurge / maxUnavailable、minReadySeconds、revisionHistoryLimit、progressDeadlineSeconds 和 paused；Recreate 资源会按实际策略显示，不再默认假设所有 Deployment 都是 RollingUpdate。
- 为 Deployment 编辑弹窗 YAML textarea 补充字号控制，复用创建 / 详情 YAML 区的 12/13/14/16 字号选项，避免编辑模式下 YAML 字体过小无法阅读。
- 同步通过 `corepack pnpm run typecheck` 和 `corepack pnpm run build`，并在浏览器确认详情 YAML 背景、文字颜色、字号和 key 高亮已按新样式渲染。
- 优化 Pod 列表指标展示：新增 CPU / 内存列，实时 Metrics 存在时展示当前用量 / limit；未返回单行实时用量时展示 request / limit 兜底；整页没有实时 Metrics 时提示需要安装并启用 Kubernetes Metrics Server。
- 优化资源表格列设置与 Pod 行展示：列表页新增“列设置”下拉，可按资源类型记忆隐藏列；Pod 标签与操作列改为单行展示并通过横向滚动承载宽表格，减少 Pod 行自动折行。
- 同步通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build`，并通过 Playwright 验证 `/pods` CPU/内存列、Metrics Server 提示逻辑、列隐藏/恢复和 Pod 行高表现。
- 继续优化 Pod 表格交互：CPU / 内存从纯数字改为百分比徽标 + 横向用量条 + 用量/limit 文案；Pod 默认隐藏 Namespace 与标签列，恢复默认列也保持隐藏，可在列设置中手动显示；Pod 操作列移除重复“实时”按钮，保留“日志”并默认按实时日志 follow 打开。
- 补齐资源批量选择与 Pod 批量删除：表格新增选择列、全选当前筛选结果、批量操作条和批量删除确认；mock 模式下批量删除会真实移除列表数据，便于验证后续详情/列表流程。
- 同步通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build`，并通过 Playwright 验证 `/pods` 默认列、列设置恢复、条形指标、实时日志入口和批量删除闭环。
- 按 Kubernetes 资源窗口展示习惯调整 Pod CPU / 内存列：上方展示 `req` / `limit` 资源配额，下方展示用量条、百分比和当前 Metrics 用量；未返回 Metrics 时显示“未返回”，不再用 request 伪装实时用量百分比。
- 同步通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build`，并通过 Playwright 复核 `/pods` 指标列顺序、默认隐藏列、日志入口和批量选择条。
- 优化 Pod 节点排障入口：筛选区新增“全部节点 / 节点名”下拉，可直接查看指定节点上的 Pod；节点列补充节点内网 IP，并在存在外网 IP 时展示外网 IP，缺失时不显示空占位。
- 同步通过 `corepack pnpm run typecheck`、`corepack pnpm run lint:check`、`corepack pnpm run build`，并通过 Playwright 验证节点筛选、内网 IP 展示和外网 IP 条件展示。
- 继续压缩 Pod 表格密度：CPU / 内存列将 `current` 用量下沉到图表下方，列宽收敛到更紧凑的资源窗口布局；Pod 操作列降低最小宽度和单元格内边距，减少“详情 / 编辑 / 日志 / 终端 / 删除”按钮后的无效留白。
- 按源码参考图继续收敛 Pod CPU / 内存列：表格内只展示轻量用量条和当前用量，request / limit 移入 hover 浮层，浮层展示 `Usage / Request / Limit`，进度条中的细竖线表示 request 位置。
- 修正 Pod CPU / 内存列常驻数值：表格内显示百分比，真实用量继续保留在 hover 浮层的 `Usage` 中，避免把 `190m` / `384Mi` 直接放在列内造成语义偏差。
- 优化 Pod 列表到详情的进入方式：Pod 名称改为直接打开 Pod 详情，Pod 操作列移除“详情”按钮；节点名称改为可点击跳转到 Node 页面并自动打开对应 Node 详情。
- 重构 Pod 详情概览布局：Pod 概览改为专属排障视图，首屏展示状态、就绪、重启、节点、Pod IP、年龄；下方展示容器表、核心信息，右侧展示事件、关联资源、端口、标签和注解摘要，避免继续用通用字段卡片铺满详情。
- 继续收敛 Pod 详情弹窗：Pod 详情顶部仅保留“基本信息 / 事件 / YAML”页签，状态、标签与注解、关联资源改为概览区摘要；YAML 页签支持直接编辑保存并同步当前资源摘要；Pod 操作列移除“编辑”，由详情 YAML 承担编辑入口。
- 补齐 Pod 容器排障叠层：容器名称可点击打开容器详情，展示镜像、状态、重启、端口、CPU、内存、环境变量、volumeMounts、ConfigMap、Secret；卷与挂载作为 Pod 概览独立分组展示，关联资源会根据 owner / ownerReferences 同时推导 Deployment 与 ReplicaSet。
- 增强 Pod mock YAML：Pod 模拟 YAML 会生成 env、volumeMounts 和 volumes，便于在前端直接验证容器详情、卷挂载、ConfigMap / Secret 关联展示。
- 继续优化 Pod 容器详情叠层：容器详情改为单容器视角的“详情 / 环境变量 / 挂载”分段视图，详情页展示 image、imagePullPolicy、state、ports、resources、imageID、containerID 和当前容器 volumeMounts；环境变量和挂载页签只解析当前容器，不再混用整个 Pod 的汇总字段。
- 为 Pod 详情新增“挂载”页签：在 Pod 详情顶部与事件同级展示 Pod volumes 总览和每个容器的 volumeMounts 矩阵；ConfigMap、Secret、PVC 来源可点击跳转到对应资源详情，emptyDir、hostPath 等 Pod 内部卷以不可跳转摘要展示。
- 继续打磨 Pod 容器详情数据：容器详情的“详情”页移除重复 Volume Mounts 区块，挂载只保留在“挂载”页签；Pod mock YAML 增强为多容器示例，补充 resources、ConfigMap/Secret/emptyDir 挂载、imageID、containerID 和 startedAt，便于验证真实展示效果。
- 按 Pod 详情模式重构 Deployment 详情：Deployment 名称可直接进入详情，首屏展示状态、期望副本、已更新、可用、不可用和年龄；主体展示 Pod 模板容器、发布策略与 selector、模板卷、Conditions、关联 ReplicaSet/Pod/Service/EndpointSlice、镜像和标签。字段取舍参考 Kubernetes 官方 Deployment 文档和 apps/v1 DeploymentStatus，不照搬 Pod 的 Pod IP、节点、重启、containerID 等单实例字段。
- 继续收敛 Deployment 详情：详情弹窗标题改为 `Deployment 资源详情` 等资源 Kind 标识；Deployment 顶部页签只保留基本信息、事件和可编辑 YAML；移除右侧重复镜像摘要，标签区补充注解；发布策略字段加宽避免参数和值挤压；Pod 模板容器名称可点击打开模板容器详情，展示模板 image、pullPolicy、ports、resources、env 和 volumeMounts，不展示 Pod 运行时 imageID/containerID。
- 继续收敛 Deployment 列表操作列：Deployment 不再展示重复的“详情”和“编辑”按钮，名称点击进入详情，详情 YAML 承担编辑保存入口；同步收窄 Deployment 操作列宽度，减少按钮移除后的留白。
- 为资源详情弹窗补充统一“监控”页签：Pod 和 Deployment 共用同一套 CPU / 内存指标面板、用量条、百分比和降级提示；Pod 展示当前实例，Deployment 按 selector / 关联 Pod 聚合指标，避免后续资源各自实现不同监控样式。
- 增强 Pod 挂载模拟数据：两个 `api-server` Pod 的 mock YAML 补充 ConfigMap、Secret、emptyDir、Memory emptyDir 和 projected serviceAccount token 卷；Pod 全局挂载页展示 volumes 与容器挂载矩阵，容器详情“挂载”页只展示当前容器自身 volumeMounts。
- 优化 Pod 日志与终端弹窗：日志和终端预览改为浅色可选中文本代码块，避免选中文字低对比；终端入口改为先选择容器和进入命令，内置 `/bin/sh`、`/bin/bash`、`/busybox/sh`、`/bin/ash` 与自定义命令，再提交终端连接请求。
- 重构资源详情监控页签为时间序列图表：顶部提供时间范围、刷新间隔和目标选择；Pod 可选择 All Containers 或单个容器，Deployment 可选择 All Pods 或单个关联 Pod；主体展示 CPU、Memory、Network、Disk I/O 四张图，并在 hover 时展示对应时刻的指标 tooltip。
- 优化监控时间序列 hover：每张图支持多个时间点悬停，hover 竖线和点位跟随时间点移动；tooltip 在同一时刻同时展示 CPU、Memory、Network In/Out、Disk Read/Write，便于跨指标对比分析。

## 2026-05-28 v0.1.2

### 检查与版本

- 完成 `/check` 前端复检：Deployment 完整表单 `form->yaml->form` 往返通过，Service、ConfigMap、Secret、PVC 创建表单与 YAML 往返通过。
- 修复容器挂载 `readOnly` 映射：ConfigMap / Secret / PVC / emptyDir 挂载勾选只读时生成 `readOnly: true`，未勾选时不写入；YAML 中的 `readOnly: true` 可回填到表单勾选态，取消勾选会清除旧 YAML 保留字段。
- 优化创建弹窗 YAML 模式：支持 `---` 分隔的多文档 YAML，逐个校验当前资源类型对象；顶部资源切换条可选择当前表单编辑对象，表单修改只替换对应文档，提交时保留完整 YAML 并携带资源数量，便于后端批量创建。
- 补齐 mock 创建闭环：YAML 创建在 mock 模式下会解析资源对象并写入列表；多文档 YAML 会新增多条模拟资源，详情页 YAML 保留提交内容；页面默认集群与 mock 数据源统一，创建后清空过滤并切到可见范围，便于验证完整创建流程。
- 修复 Deployment 创建后不可见的 mock 过滤问题：资源列表将 `status=all` 视为无状态过滤；创建资源的列表摘要同步携带提交 YAML，浏览器已验证批量创建 4 个 Deployment 后可在列表与详情 YAML 中看到对应资源。
- 优化资源详情展示时机：资源列表初始态不再自动选中第一条资源，也不直接渲染详情区域；仅点击操作列“详情”后展示当前资源详情。
- 优化资源详情承载方式与排障链路：详情改为弹窗展示；Deployment 关联资源补齐 ReplicaSet、Pod、Service、EndpointSlice 模拟数据；状态就绪值可跳转到 Pod 列表并自动按当前工作负载筛选关联 Pod。
- 收敛 Deployment 操作列与高风险确认：移除列表行独立 YAML 操作，YAML 仅保留在详情和编辑入口；扩缩容、重启、镜像更新、回滚和删除改为单次确认弹窗，不再向用户重复展示审计提示。
- 同步通过 `corepack pnpm run typecheck` 和 `corepack pnpm run build`，前端检查门禁更新为通过；整体门禁保持未通过，原因是后端真实 API、SQLite、Kubernetes adapter、服务端 RBAC 与审计尚未实现。
- 将前端包版本、默认应用版本和项目状态检查点同步为 `v0.1.2`。

## 2026-05-27 v1.0.0

### 新增功能

- 确立 Kubernetes Web 管理平台 v1.0 需求基线，覆盖多集群管理、资源管理、日志终端、YAML 管理、平台 RBAC、操作审计和基础安全保护。

### 优化改进

- 前端资源创建体验增加首批表单化能力：Deployment、Service、ConfigMap、Secret、PVC 支持表单填写与 YAML 双向同步；其他资源暂保留 YAML 创建，待首批体验确认后扩展。
- 根据浏览器批注优化创建弹窗：表单创建 / YAML 编辑选中态更明显，创建弹窗在窄视口不再横向滚动，Deployment 表单补充资源 requests / limits、健康检查、ServiceAccount 和 nodeSelector，并提示复杂 YAML 字段保留在高级配置中。
- 继续优化 Deployment 创建表单：资源限制拆分为 CPU / 内存切换面板，健康检查拆分为 Readiness / Liveness 切换面板并补充延迟、周期、超时、失败阈值和成功阈值，新增 ConfigMap / Secret / emptyDir 挂载配置分组，YAML 编辑区改为随内容自适应高度。
- 继续优化 Deployment 高级创建表单：所有配置分组支持折叠收起，调度策略移动到挂载配置后并拆分 nodeSelector、节点亲和、Pod 亲和、Pod 反亲和和容忍配置，补充 Startup 探针、PostStart / PreStop 生命周期钩子、Web/API/Worker/DB YAML 模板、YAML 字号调整和 ConfigMap / Secret 选择/创建入口。
- 继续优化 Deployment 创建弹窗：ServiceAccount 归位到 Pod 模板，调度策略 key 输入区加宽，YAML 模板明确为可编辑预填并保留已修改镜像，挂载表单只选择已有 ConfigMap / Secret，创建入口切换到真实 ConfigMap / Secret 表单，折叠按钮统一右对齐，并放大弹窗工作区以支持表单和 YAML 并排编辑。
- 继续优化 Deployment 创建体验：创建弹窗增加常规 / 超宽模式以适配不同屏幕；ConfigMap / Secret 改为在当前 Deployment 表单上叠加创建，取消不丢失主表单，创建成功后自动回填挂载名称；YAML 区增加本地语法解析、Kubernetes 基础字段校验、注释忽略说明和高亮预览。
- 继续优化 Deployment Pod 模板：ServiceAccount 调整到环境变量之后；Pod 模板增加普通容器 / Init 容器切换，Init 容器支持基础信息、command / args、环境变量、资源 requests / limits 和挂载配置，并按 Kubernetes 官方语义不配置普通 Init 容器不支持的探针与生命周期钩子；YAML 校验提示合并到 YAML 编辑区，去除独立黑底高亮预览块。
- 继续优化 Deployment Pod 模板上下文：切到 Init 容器时隐藏普通容器专属的资源限制、健康检查、挂载与配置、生命周期钩子分组，只保留 Init 容器自身资源 / 挂载配置；调度策略改名为 Pod 调度策略并说明其作用于整个 Pod。
- 继续优化 Deployment 普通容器编辑体验：普通容器改为与 Init 容器一致的容器列表 + 配置页签模式，支持多个普通容器分别配置基础、端口、环境变量、资源、健康检查、挂载和生命周期；YAML 右侧仅保留语法有效 / 错误状态提示，移除面向用户无必要的基础字段、准入兜底和注释说明胶囊。
- 继续优化 Deployment 表单分类语义：基本信息只保留名称和 Namespace；标签 / 注解独立为元数据分组；ServiceAccount 与自动挂载 Token 独立为 Pod 安全身份分组；Deployment 更新策略独立配置 RollingUpdate/Recreate、maxSurge、maxUnavailable、minReadySeconds、revisionHistoryLimit、progressDeadlineSeconds 和 paused，默认不写入 YAML；创建弹窗分类默认收起。
- 继续优化 Deployment 创建表单信息架构：按 Deployment、Deployment 更新策略、Pod 模板、Pod 安全、Pod 存储、Pod 调度策略顺序重排；Deployment 内部拆分基本信息 / 元数据信息，基本信息包含名称、Namespace 和副本数，默认标签 key 为 app、注解为空；Pod 安全补充 Pod / 容器 securityContext；Pod 存储补充 PVC 挂载和叠层创建；Pod 调度策略合并为节点调度、Pod 调度和容忍三类。
- 继续优化 Deployment Pod 存储模型：Pod 存储卷只生成 `spec.template.spec.volumes`，普通容器和 Init 容器的挂载页签从 Pod Volume 中选择并分别生成 `volumeMounts`；ConfigMap / Secret / PVC 叠层创建统一为“新建并选中”语义。
- 继续优化 Deployment 创建表单结构：容器 SecurityContext 从 Pod 安全移入 Pod 模板的普通容器“容器安全”页签，并按当前普通容器生成 `securityContext`；Pod 模板折叠态补充说明，创建弹窗默认常规宽度，YAML 区移除字号控件。
- 继续精简 Deployment 创建默认 YAML：默认模板和模板切换只生成最小 Deployment 骨架，切换 Web / API / Worker / DB 模板时同步替换对应镜像；普通容器端口配置并入“基础”页签，“容器安全”页签简化为“安全”。
- 继续优化 Deployment 普通容器编辑入口：“添加普通容器”移动到普通容器名称列表右侧，并改为红色按钮，减少新增入口的查找成本。
- 继续补齐 Deployment Pod 级镜像拉取和网络配置：普通容器端口默认空态，`hostPort` 显式勾选后写入；Pod 安全补充 `imagePullSecrets`；Pod 调度补充 `hostNetwork`、`dnsPolicy` 和 `dnsConfig`。
- 继续优化 Deployment Init 容器编辑入口与安全配置：“添加 Init 容器”移动到 Init 容器名称列表右侧并使用红色按钮；普通容器和 Init 容器的移除当前容器按钮统一改为红色危险色；普通 Init 容器补充 `securityContext` 表单和 YAML 解析/生成能力，并按 Kubernetes 官方语义不增加 lifecycle、readinessProbe、livenessProbe 或 startupProbe。
- 继续重构 Deployment Pod 网络与调度分类：`hostNetwork`、`dnsPolicy`、`dnsConfig` 从 Pod 调度中拆出，作为 Pod 安全下方的独立“Pod 网络”；Pod 亲和 / 反亲和独立为“Pod 调度”；`nodeSelector`、节点亲和和污点容忍独立为“节点调度”，并将节点标签选择、节点亲和、污点容忍的表头、空态和按钮改为中文化表达。
- 修复 Deployment YAML 编辑模式模板切换问题：在 YAML 编辑中选择 Web / API / 后台任务 / 轻量 DB 模板时保持当前 YAML 编辑模式，只更新表单状态并重新生成 YAML，不再跳回表单创建后覆盖模板内容。
- 优化 Deployment 调度规则表单布局：Pod 亲和 / 反亲和、节点标签选择、节点亲和和污点容忍的添加按钮统一使用红色主动作；每条规则的删除按钮下移到第二行右侧，字段区按容器宽度换行，避免在常规弹窗宽度下横向溢出。
- 优化 Deployment YAML 编辑区标题布局：将 YAML 语法状态胶囊移动到 YAML 标题旁边，说明文案独立放到下一行，避免状态提示贴近右侧造成文字截断观感。
- 修复 Deployment 调度规则在宽视口窄容器中仍横向溢出的问题：去除按 `2xl` 视口触发的多列规则布局，固定为容器内两列换行；恢复 YAML 字号 12/13/14/16 调整能力，并同步作用于高亮层和输入层。
- 继续收敛 Deployment YAML 工具栏：将字号控制移动到标题行右侧，左侧增加当前表单模式 / YAML 模式徽标，并移除模板描述灰底框和 YAML 下方重复说明文案。
- 优化 Deployment Pod 安全与添加入口：`imagePullSecrets` 从文本输入改为 Secret 多选区，多选后自动以逗号写回并生成 YAML；支持新建 Secret 并自动选中；DNS 选项、Pod 存储卷添加和新建并选中按钮统一为红色主动作。
- 继续优化 Deployment Pod 安全与挂载入口：`imagePullSecrets` 改为下拉式多选，仅展示镜像仓库认证 Secret（`kubernetes.io/dockerconfigjson` / `kubernetes.io/dockercfg`）并保留新建 Secret 自动选中；移除 Pod 存储卷、普通容器挂载和 Init 容器挂载行内重复“新建并选中”入口，统一使用底部新建入口。
